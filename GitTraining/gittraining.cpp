//gitの個人練習
//ブランチ
//統合
//コントローラー接続テスト
//チームエクスプローラー⇒ブランチ⇒新しいブランチ⇒フェッチ（ブランチに追加した変更がブランチに保存される）
//⇒コミット？⇒マージ（元のファイルに反映させる）
//ブランチにコミットは出来ない⇒元のファイルにマージ（反映）後、コミットでブラウザに変更がかかる
//end


#include <stdio.h>

int main(void)
{
	//ブランチ確認
	printf("ブランチ確認\n");
	printf("ブランチ確認2\n");
	printf("ブランチ確認3.5\n");
	printf("反映テスト");
	rewind(stdin);
	getchar();
	return 0;
}